import React, { useState } from 'react';

const Player = () => {
  const [isPlaying, setIsPlaying] = useState(false);

  return (
    <div>
      <p>hi!</p>
    </div>
  );
};

export default Player;
